{
  "servers": {
    "Render Unto Caesar": {
      "type": "stdio",
      "command": "docker",
      "cwd": "${workspaceFolder}",
      "args": [
        "run",
        "--rm",
        "-i",
        "-e",
        "RUC_MCP_LOG_LEVEL=DEBUG",
        "mightydatainc/ruc-mcp:local"
      ]
    }
  }
}
